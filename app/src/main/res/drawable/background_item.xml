<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Градиентный фон из второго кода -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <gradient
                android:startColor="#020202"
                android:centerColor="#5D1D24"
                android:endColor="#CA1515"
                android:angle="11" />
        </shape>
    </item>

    <!-- Тёмная основа из первого кода -->
    <item android:bottom="4dp" android:left="4dp" android:right="4dp" android:top="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="4dp" />
            <solid android:color="#171318" />
        </shape>
    </item>

    <!-- Двойная рамка из первого кода -->
    <item android:bottom="10dp" android:left="10dp" android:right="10dp" android:top="10dp">
        <shape android:shape="rectangle">
            <corners android:radius="2dp" />
            <stroke
                android:width="3dp"
                android:color="#661616"
                android:dashWidth="8dp"
                android:dashGap="4dp" />
        </shape>
    </item>

</layer-list>